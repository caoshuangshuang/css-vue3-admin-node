/// <reference types="node" />
interface Codec {
    encode(buffer: Buffer): string;
    decode(s: string): Buffer;
    bytesForLength(n: number): number;
    padChar(): string;
}
declare function createCharset(s: string): string;
declare function codec(charset: string): Codec;
export { Codec, createCharset, codec };
